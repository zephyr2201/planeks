build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: python manage.py migrate && python manage.py collectstatic --noinput && python manage.py runserver 0.0.0.0:$PORT
  worker: celery -A core worker -B --loglevel=INFO --concurrency=1